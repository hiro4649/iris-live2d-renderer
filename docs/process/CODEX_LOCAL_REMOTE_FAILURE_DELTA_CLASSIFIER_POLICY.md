<!-- CODEX_QUALITY_HARNESS_FILE v1.0.7 -->
# Local Remote Failure Delta Classifier Policy

Local pass never implies remote pass. When local evidence passes and remote evidence fails, the harness must classify the delta as governance body, stale event payload, stale artifact, environment, actual product, remote npm normalization, or unknown. Unknown requires a precise safe next action.
